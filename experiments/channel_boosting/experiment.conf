experiment_name = "ChannelBoosting"

local_setup_cmd = "tribler_experiment_setup.sh"

build_swift = True

PYTHONOPTIMIZE = "yup"

local_instance_cmd = 'process_guard.py -t 3900 -i 5 -c "wrap_in_vnc.sh gumby/experiments/channel_boosting/tribler_idle_run.py"'

tracker_cmd = ""
# Enable use of virtualenv when running on the local machine (will be always used on the DAS4)
use_local_venv = True
# Enable use of systemtap (Will be never used on DAS4)
use_local_systemtap = False

#Custom variable to control the experiment execution time in seconds
TRIBLER_EXECUTION_TIME = 300
